export type PersonalType = {
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
};

export type SchoolType = {
  schoolName: string;
  field: string;
  educationStart: Date;
  educationEnd: Date;
};

export type WorkType = {
  workplace: string;
  position: string;
  responsibilities: string;
  workStart: Date;
  workEnd: Date;
};
